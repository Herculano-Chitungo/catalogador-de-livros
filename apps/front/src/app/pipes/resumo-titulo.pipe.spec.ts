import { ResumoTituloPipe } from './resumo-titulo.pipe';

describe('ResumoTituloPipe', () => {
  it('create an instance', () => {
    const pipe = new ResumoTituloPipe();
    expect(pipe).toBeTruthy();
  });
});